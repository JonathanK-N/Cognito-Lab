{
  "name": "@cognitolab/sim-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "dev": "wasm-pack build --target web --out-dir pkg --dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wasm-pack": "^0.12.0"
  }
}

