# Variables d'environnement pour Railway
# Copier ces variables dans Railway Dashboard → Variables

# ============================================
# API Service Variables
# ============================================

# Base de données (utiliser la référence Railway)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Secrets JWT (générer avec: openssl rand -base64 32)
JWT_SECRET=your-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key

# URL du client web (remplacer par votre URL Railway)
CLIENT_URL=https://your-web-app.railway.app

# Port (Railway assigne automatiquement)
PORT=4000

# Environnement
NODE_ENV=production

# ============================================
# Web Service Variables
# ============================================

# URL de l'API (remplacer par votre URL Railway)
NEXT_PUBLIC_API_URL=https://your-api.railway.app/api

# URL Socket.IO (remplacer par votre URL Railway)
NEXT_PUBLIC_SOCKET_URL=https://your-api.railway.app

# Port (Railway assigne automatiquement)
PORT=3000

# Environnement
NODE_ENV=production

